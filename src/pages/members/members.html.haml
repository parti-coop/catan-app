%ion-header
  %ion-toolbar
    %ion-buttons{ 'start': true}
      %button{ 'ion-button': true, '(click)': 'dismiss()' }
        X
    %ion-title
      .title-member 멤버 {{ parti.members_count }}
      .title-parti {{ partiTitle() }}
%ion-content{ 'padding': true }
  %ion-list
    %ion-item{ '*ngFor': 'let member of members' }
      .member
        .member-image{ '[ngStyle]': "{ 'background-image': 'url(' + member.user.image_url + ')' }" }
        .member-name {{ member.user.nickname }}
